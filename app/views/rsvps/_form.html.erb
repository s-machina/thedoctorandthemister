<%= form_for(@rsvp) do |f| %>
  <% if @rsvp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rsvp.errors.count, "error") %> prohibited this rsvp from being saved:</h2>

      <ul>
      <% @rsvp.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :guest_name %><br />
    <%= f.text_field :guest_name %>
  </div>
  <div class="field">
    <%= f.label :companion_name %><br />
    <%= f.text_field :companion_name %>
  </div>
  <div class="field">
    <%= f.label :is_joining %><br />
    <%= f.check_box :is_joining %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
